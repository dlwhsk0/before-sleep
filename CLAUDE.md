# 건강 기록 (health-tracker)

수면 시간과 몸무게를 매일 기록하고, 최소한의 통계만 보여주는 개인용 웹앱.
"기능을 적게 넣는다"가 이 프로젝트의 핵심 원칙이다. 새 기능을 제안할 땐
이 원칙에 부합하는지 먼저 따져볼 것.

## 무엇을 만드는가 (스코프)

매일 두 값을 기록한다: **수면 시간(시간)**, **몸무게(kg)**.
보여주는 것은 딱 이것만:

- 데일리 기록값
- 전일 대비 증감 (수면·몸무게 둘 다)
- 첫날 대비 증감 (몸무게만)

이 목록에 없는 기능(칼로리, 운동, 목표 설정, 알림 등)은 요청받기 전까지 만들지 않는다.

## 기술 스택

- **Vite + React 19 + TypeScript**
- **저장소: IndexedDB** (`idb` 래퍼) — 백엔드 없음, 데이터는 브라우저 로컬에만 저장
- **차트: Recharts**
- **스타일: Tailwind CSS v4** (`@tailwindcss/vite` 플러그인, `@import 'tailwindcss'`)
- **PWA** (단계 4에서 추가 예정)

여러 기기 동기화가 필요해지기 전까지 백엔드는 도입하지 않는다.

## 데이터 모델

```ts
type DailyRecord = {
  date: string        // "YYYY-MM-DD", 하루 한 개 (기본 키)
  sleepHours: number  // 예: 7.5
  weightKg: number    // 예: 68.2
}
```

### 통계 계산 규칙

- **전일 대비**: 달력상 전날이 아니라 **직전에 기록이 있는 날** 기준으로 계산한다.
  (중간에 기록을 빼먹어도 자연스럽게 이어지도록.)
- **첫날 대비**: 몸무게만, `오늘 − 가장 오래된 기록`.
- 비교 대상이 없으면(첫 기록) 증감은 표시하지 않는다.

## 컨벤션

- 날짜 키 포맷은 항상 `YYYY-MM-DD` 로컬 날짜. UTC 변환으로 날짜가 밀리지 않게 주의.
- 몸무게/수면은 소수점 한 자리까지.
- UI 텍스트는 한국어.
- 커밋은 각 단계(아래) 단위로, 의미 있는 메시지로 남긴다.

## 개발 명령어

- `npm run dev` — 개발 서버
- `npm run build` — 타입체크(`tsc -b`) + 프로덕션 빌드
- `npm run lint` — oxlint

## 빌드 단계 (진행 상황)

- [x] 단계 0 — 스캐폴딩 + 이 CLAUDE.md
- [ ] 단계 1 — 데이터 계층(IndexedDB) + 통계 로직
- [ ] 단계 2 — 기록 입력 UI + 목록
- [ ] 단계 3 — 통계/차트 화면
- [ ] 단계 4 — PWA + 마무리

---

_이 파일은 Claude Code가 매 세션 자동으로 읽는 "프로젝트 메모리"다.
결정사항·컨벤션·스코프가 바뀌면 여기부터 업데이트할 것._
